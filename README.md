# Many-to-One RNN (SMS Spam Detection)

A web application to detect SMS spam using a Many-to-One RNN model.

## How to Run

1. Install requirements: `pip install -r requirements.txt`
2. Run app: `streamlit run app.py`
